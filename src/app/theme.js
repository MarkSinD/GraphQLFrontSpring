import { createTheme  } from '@mui/material/styles';

const customTheme = createTheme ({
    palette: {
      secondary: {
        main: "#fffff"
      },
			primary: {
        main: "#00000"
      }
    }
  });

export default customTheme;